import { DefaultTheme } from "styled-components";

const colors = {
  primary_green: "#3B872F",
  primary_gray: "#979C9E",
  primary_black: "#000000",
  sub_red: "#D3180C",
  sub_yello2: "#FFB323",
};

const theme: DefaultTheme = {
  colors,
};

export default theme;
